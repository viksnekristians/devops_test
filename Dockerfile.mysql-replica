FROM mysql:8.0

# Copy init scripts and replica config into the image
COPY docker/mysql/init/ /docker-entrypoint-initdb.d/
COPY docker/mysql/conf/replica.cnf /etc/mysql/conf.d/replica.cnf

# Make sure scripts are executable
RUN chmod +x /docker-entrypoint-initdb.d/*.sql 2>/dev/null || true