version: '3.8'

services:
  php-app:
    build: .
    container_name: php-hello-world
    ports:
      - "8080:80"
    volumes:
      - .:/var/www/html
    environment:
      - APACHE_DOCUMENT_ROOT=/var/www/html